<p style="color: green"><%= notice %></p>

<%= render @notepad %>

<div>
  <%= link_to "Edit this notepad", edit_notepad_path(@notepad) %> |
  <%= link_to "Back to notepads", notepads_path %>
  <%= link_to 'carts', notepads_carts_path(@notepad) %>
  <%= button_to "Destroy this notepad", [@notepad], method: :delete %>
</div>
