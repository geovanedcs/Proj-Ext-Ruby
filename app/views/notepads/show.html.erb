<p style="color: green"><%= notice %></p>

<%= render @notepad %>

<div>
  <%= link_to "Edit this notepad", edit__notepad_path(@notepad) %> |
  <%= link_to "Back to notepads", _notepads_path %>

  <%= button_to "Destroy this notepad", [, @notepad], method: :delete %>
</div>
