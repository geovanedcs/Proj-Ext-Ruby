<p style="color: green"><%= notice %></p>

<%= render @cart %>

<div>
  <%= link_to "Edit this cart", edit__cart_path(@cart) %> |
  <%= link_to "Back to carts", _carts_path %>

  <%= button_to "Destroy this cart", [, @cart], method: :delete %>
</div>
